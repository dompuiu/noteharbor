PORT=3001

# Optional scraping override. Defaults to http://localhost:9222.
# The server reads the current HTML from a matching tab already open in this browser.
# NOTE_HARBOR_BROWSER_CDP_URL=http://localhost:9222

# Optional Note Harbor overrides.
# NOTE_HARBOR_DATA_DIR=../../../data
# NOTE_HARBOR_WEB_DIST_DIR=../web/dist
# NOTE_HARBOR_DISABLE_SCRAPING=false
