PORT=3001

# Persistent browser profile reused by both the PMG warmup window and the scraper.
PMG_BROWSER_PROFILE_DIR=./storage/browser_profiles/pmg

# Optional Note Harbor overrides.
# NOTE_HARBOR_DATA_DIR=../../../data
# NOTE_HARBOR_WEB_DIST_DIR=../web/dist
# NOTE_HARBOR_DISABLE_SCRAPING=false
